@foreach ($user as $users)
    <div>
        <h2>{{ $user->name }}</h2>
        <h2>{{ $user->name }}</h2>
        <p>Email: {{ $user->email }}</p>
        <
        <!-- Add other customer details here -->
    </div>
@endforeach